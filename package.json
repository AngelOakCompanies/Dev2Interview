{
  "name": "dev2interview",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "test": "tsc && node ./dist/index.js",
    "build": "tsc",
    "start:debug": "npm run build && node --enable-source-maps dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.x",
    "typescript": "^4.3.5"
  }
}
